const Title = () => {
  const titleStyle = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  };

  return <h1 style={titleStyle}>2024 파리 올림픽 메달 기록</h1>;
};

export default Title;
